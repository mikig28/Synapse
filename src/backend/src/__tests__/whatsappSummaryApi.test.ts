import { describe, it } from '@jest/globals';

describe.skip('WhatsApp Summary API integration', () => {
  it('is skipped – legacy Vitest suite requires porting', () => {
    // TODO: port to Jest or run under Vitest separately
  });
});
